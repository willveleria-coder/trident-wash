'use client';

// BookingSection removed per client request — calendar booking component deleted.
// This file is kept as an empty export to avoid import errors.

export default function BookingSection() {
  return null;
}